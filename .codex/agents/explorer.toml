name = "explorer"
description = "Read-only agent for exploring and mapping the codebase."

model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "read-only"

developer_instructions = """
Explore the codebase without modifying files.
Locate relevant files, symbols, dependencies, and execution paths.
Return concise findings to the parent agent.
"""
